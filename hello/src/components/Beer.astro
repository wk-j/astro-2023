---
import { Button, onClick } from "./MyReact";

type Props = {
    name: string;
};

const { name } = Astro.props;
---

<html>
    <body>
        <h1>Hello, {name}!</h1>
        <Button client:idle text="Click" />
    </body>
</html>
